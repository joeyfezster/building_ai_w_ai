# Compute

- Local CPU default: run smoke config (`configs/dqn_minipong.yaml`).
- Remote SkyPilot tasks in `infra/compute/skypilot` for longer runs.
- Always run smoke locally before remote submission to avoid wasted GPU time.

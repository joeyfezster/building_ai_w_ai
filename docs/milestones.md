# Milestones

## Milestone 1: Dev-team operating model + enforced validations
- Agent roles documented in `/agents/dev_team`.
- Workflow and PR template enforce `make validate` and `make verify-learning`.

## Milestone 2: DQN learns MiniPong from pixels with proof artifacts
- Deterministic pixel environment.
- DQN training/eval/checkpoint/video pipeline.
- Learning verification and dashboard over real artifacts.

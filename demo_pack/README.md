# Demo Pack

Contains training videos, packaged artifacts, and dashboard-ready data.

## Recreate
- Run training/eval: see `docs/demo.md`.
- Videos are in `demo_pack/training_videos` or `artifacts/<run_id>/videos`.
- Dashboard reads `artifacts/<run_id>`.

{
  "$defs": {
    "ConceptLocation": {
      "description": "A specific code location referenced by a review concept.\n\nLocations carry one of two distinct intents, distinguished by `context`:\n\n- `context=False` (default) \u2014 the **anchor**: this code is what the\n  finding is ABOUT. The file MUST be in the PR's diff. Live-pack\n  validation rejects findings where every anchor location is\n  out-of-diff (`finding-location-mismatch`).\n\n- `context=True` \u2014 a **cross-reference**: this is a related file the\n  reader should know about (where the fix should go, an analogous\n  pattern, a related test, a contract spec). The file does NOT need\n  to be in the diff. Live-pack validation skips these for the\n  in-diff check. Renderer marks them visually as supplementary.\n\nEvery finding must have at least one anchor location pointing into\nthe diff. Out-of-diff anchors fail validation; out-of-diff context\nreferences are accepted.",
      "properties": {
        "file": {
          "description": "File path relative to repo root",
          "title": "File",
          "type": "string"
        },
        "lines": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Line range, e.g. '42-58' or '12'. None for file-level findings.",
          "title": "Lines"
        },
        "zones": {
          "description": "Zone IDs from zone-registry.yaml that this file belongs to",
          "items": {
            "type": "string"
          },
          "title": "Zones",
          "type": "array"
        },
        "comment": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Location-specific comment or code snippet context",
          "title": "Comment"
        },
        "context": {
          "default": false,
          "description": "If True, this is a cross-reference (out-of-diff context: where the fix should go, an analogous pattern, a related test). The file is not validated against the PR diff. If False (default), this is an anchor \u2014 the file MUST be in the diff.",
          "title": "Context",
          "type": "boolean"
        }
      },
      "required": [
        "file"
      ],
      "title": "ConceptLocation",
      "type": "object"
    },
    "FindingCategory": {
      "description": "Categories for review concept findings.",
      "enum": [
        "code-health",
        "security",
        "test-integrity",
        "adversarial",
        "architecture",
        "cross-cutting",
        "rbe"
      ],
      "title": "FindingCategory",
      "type": "string"
    },
    "Grade": {
      "description": "Valid review grades. N/A is explicitly excluded.",
      "enum": [
        "A",
        "B+",
        "B",
        "C",
        "F"
      ],
      "title": "Grade",
      "type": "string"
    }
  },
  "description": "A single concept-level finding from a review agent.\n\nEach agent writes one ReviewConcept per line to a .jsonl file.\nThe assembler transforms these into AgenticFinding objects for the\nreview pack.\n\nIdentity: The agent name is derived from the .jsonl filename\n(e.g., pr5-code-health-abc12345-def67890.jsonl \u2192 \"code-health\"),\nNOT stored in this model.",
  "properties": {
    "concept_id": {
      "description": "Unique identifier within this agent's output. Convention: {category}-{seq}, e.g. 'security-1', 'code-health-3'",
      "title": "Concept Id",
      "type": "string"
    },
    "title": {
      "description": "One-line summary of the finding",
      "maxLength": 200,
      "minLength": 1,
      "title": "Title",
      "type": "string"
    },
    "grade": {
      "$ref": "#/$defs/Grade",
      "description": "Quality grade: A (clean), B+ (minor), B (warnings), C (issues), F (critical)"
    },
    "category": {
      "$ref": "#/$defs/FindingCategory",
      "description": "Finding category \u2014 should match the agent's paradigm"
    },
    "summary": {
      "description": "Brief explanation of the finding (plain text)",
      "minLength": 1,
      "title": "Summary",
      "type": "string"
    },
    "detail_html": {
      "description": "Full explanation with evidence (HTML-safe content)",
      "minLength": 1,
      "title": "Detail Html",
      "type": "string"
    },
    "locations": {
      "description": "Code locations relevant to this finding. At least one location required. Single-location concepts are valid.",
      "items": {
        "$ref": "#/$defs/ConceptLocation"
      },
      "minItems": 1,
      "title": "Locations",
      "type": "array"
    }
  },
  "required": [
    "concept_id",
    "title",
    "grade",
    "category",
    "summary",
    "detail_html",
    "locations"
  ],
  "title": "ReviewConcept",
  "type": "object"
}
